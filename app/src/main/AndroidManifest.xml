<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="xten.cryptoxage">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".FastCalculatorActivity"
            android:label="@string/quickcon">
        </activity>
        <activity android:name=".WelcomeActivity">
        <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />

            </intent-filter>
        </activity>
        <activity android:name=".ExchangeConvertActivity">
            <meta-data
                android:name="3-Way Converter"
                android:value="com.xten.android.cryptoxchange.ExchangeRateActivity" />
        </activity>
        <activity
            android:name=".ExchangeRateActivity"
            android:launchMode="singleTask"
            android:theme="@style/AppTheme.NoActionBar"/>
        <activity android:name=".CoinActivity"
            android:label="Converter"/>
        <activity android:name=".RepositoryWebView"
            android:label="@string/repo_name"/>
       </application>

</manifest>